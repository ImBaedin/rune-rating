# `@rune-rating/config-typescript`

Shared strict TypeScript defaults for the workspace. Framework and runtime
packages should extend `base.json` and add only their required environment
types, JSX settings, and path aliases.

